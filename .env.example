# --- Production Environment Variables ---
NODE_ENV=production

# MySQL Database (Required)
# Use MYSQL_URL for a single connection string (e.g., from Railway)
MYSQL_URL=

# Or use separate fields:
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=

# App Configuration
APP_URL=
